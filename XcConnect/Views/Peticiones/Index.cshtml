@model IEnumerable<XcConnect.Models.Peticion>

@{
    ViewBag.Title = "PQRs";
}

<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row" style="margin-top: -10px;">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <div class="ibox-title">
                        <h5>PQRs</h5>
                        <input type="button" value="Nueva PQR" class="btn btn-primary pull-right" onclick="location.href='@Url.Action("Create")'" style="margin-top:-8px;" />
                    </div>
                </div>
                <div class="ibox-content">
                    <table class="table table-striped table-bordered table-hover dataTables-example">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.FechaRegistro)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Titulo)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Descripcion)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.TipoPeticion)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.FechaSolucion)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.ResueltaPor)
                                </th>
                                <th>Acciones</th>
                            </tr>
                            </thead>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.FechaRegistro)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Titulo)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Descripcion)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TipoPeticion)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.FechaSolucion)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.ResueltaPor)
                                    </td>
                                    <td>
                                        @Html.ActionLink(" ", "Edit", new { id = item.PeticionID }, new { @class = "fa fa-pencil-square-o", @title = "Editar" }) |
                                        @Html.ActionLink(" ", "Details", new { id = item.PeticionID }, new { @class = "fa fa-eye", @title = "Ver Detalle" }) |
                                        @Html.ActionLink(" ", "Delete", new { id = item.PeticionID }, new { @class = "fa fa-trash-o", @title = "Eliminar" })
                                    </td>
                                </tr>
                            }
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
    @Styles.Render("~/Content/plugins/dataTables/dataTablesStyles")
}

@section Scripts {
    @Scripts.Render("~/plugins/dataTables")

    <script type="text/javascript">
        $(document).ready(function () {

            $('.dataTables-example').DataTable({
                language: {
                    "url": "//cdn.datatables.net/plug-ins/9dcbecd42ad/i18n/Spanish.json"
                },
                pageLength: 25,
                dom: '<"html5buttons"B>lTfgitp',
                buttons: [
                    {
                        extend: 'copy',
                        title: 'PQRs',
                        exportOptions: {
                            columns: [0, 1, 2, 3, 4, 5]
                        }
                    },
                    {
                        extend: 'excel',
                        title: 'PQRs',
                        exportOptions: {
                            columns: [0, 1, 2, 3, 4, 5]
                        }
                    },
                    {
                        extend: 'pdf',
                        title: 'PQRs',
                        exportOptions: {
                            columns: [0, 1, 2, 3, 4, 5]
                        }
                    },
                    {
                        extend: 'print',
                        exportOptions: {
                            columns: [0, 1, 2, 3, 4, 5]
                        },
                        customize: function (win) {
                            $(win.document.body).addClass('white-bg');
                            $(win.document.body).css('font-size', '10px');
                            $(win.document.body).find('table')
                                    .addClass('compact')
                                    .css('font-size', 'inherit');
                        }
                    }
                ]

            });
        });

    </script>
}
